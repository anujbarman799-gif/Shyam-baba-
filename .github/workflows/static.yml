# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
<! welcome>
<html lang="Namaste">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>पानीपुरी ऑफर 🎉</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <main>
    <h1>🥳 Free Pani Puri Offer</h1>
    <p>पहले 50 कस्टमर्स को ₹50 की पानीपुरी बिलकुल FREE!</p>

    <input type="text" id="nameInput" placeholder="अपना नाम लिखें">
    <button id="submitBtn">रैंक देखो</button>

    <div id="result"></div>
  </main>

  <script src="script.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>पानीपुरी ऑफर 🎉</title>
</head>
<body>
  <h1>🥳 Free Pani Puri Offer</h1>
  <p>पहले 50 कस्टमर को ₹50 की पानीपुरी बिल्कुल FREE!</p>

  <label>अपना नाम लिखें:</label>
  <input type="text" id="nameInput">
  <button onclick="addCustomer()">रैंक देखो</button>

  <div id="result" style="margin-top:20px; font-size:18px;"></div>

  <script>
    // Local Storage में customers की list रखेंगे
    let customers = JSON.parse(localStorage.getItem("customers")) || [];

    function addCustomer() {
      let name = document.getElementById("nameInput").value.trim();
      if (!name) {
        alert("कृपया नाम डालें!");
        return;
      }

      // चेक करो नाम पहले से है या नहीं
      let index = customers.indexOf(name);
      let rank;

      if (index === -1) {
        customers.push(name);
        localStorage.setItem("customers", JSON.stringify(customers));
        rank = customers.length;
      } else {
        rank = index + 1;
      }

      let msg = `नमस्ते ${name}, आप #${rank} ग्राहक हो।`;

      if (rank <= 50) {
        msg += " 🎉 बधाई! आपको FREE पानीपुरी मिलेगी।";
      } else {
        msg += " ❌ माफ़ कीजिए, ऑफर सिर्फ पहले 50 ग्राहकों के लिए था।";
      }
      document.getElementById("result").innerHTML = msg;
    }
  </script>
</body>
</html>
